#include <iostream>
#include <string.h>
#include <string>
#include <unordered_map>
using namespace std;


int main() {
	int n,i,rep=0;
	string w, line;
	//Create unordered map with names and trips count
	unordered_map<string, int> words;
	
	cin >> w;

	while(cin.eof()){
    	//Add if not in records
        if(words.count(w) == 0) i=0;  
	}
	cout << "yes" <<endl;
        

    return 0;
}//End of main loop